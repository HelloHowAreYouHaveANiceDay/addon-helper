export default class PackageService {
    installPackage(target: string, sourcePaths: string[]){
        // copy files from the source paths to the target path
        // verify source files
        // use install handler
        // TODO: make this a event emitter to report copy status
    }   
}


